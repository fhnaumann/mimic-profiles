[project]
name = "mimic-profiles"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pathling>=9.8.0",
    "striprtf>=0.0.32",
]
